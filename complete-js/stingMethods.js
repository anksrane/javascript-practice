const abc="An anonymous function can be (and very often is) used as a function expression.";
console.log("Returns the character from the specified index:",abc.charAt(3));
str2=" I learned new thing.";
console.log(abc.concat(str2));